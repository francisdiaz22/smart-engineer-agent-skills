# Expected CI Workflow Review

Status: `NO HIGH-RISK INDICATORS FOUND`

The workflow has a branch-scoped push trigger, read-only contents permission,
and a non-privileged hosted runner. The action reference and command still
require normal maintainer review.

This fixture is inert configuration. It must not be triggered.
