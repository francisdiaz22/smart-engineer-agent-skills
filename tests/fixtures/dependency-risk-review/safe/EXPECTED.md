# Expected analysis

The manifest has no install lifecycle hook, uses an exact example version, and has a matching lockfile with placeholder integrity metadata.

Overall status: `NO HIGH-RISK INDICATORS FOUND`.

This fixture is inert and must not be installed.
